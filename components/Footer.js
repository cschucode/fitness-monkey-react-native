import React,  { useState } from 'react';
import { View, Text } from 'react-native';

const Footer = () => {
  return <Text style={{ padding: 20, color: '#bbb', backgroundColor: 'dodgerblue' }}>Footer Icons</Text>
}

export default Footer;
